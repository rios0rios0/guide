kind: 'Changed'
body: 'changed the `.chlog.yaml` example to follow the [YAML conventions](https://github.com/rios0rios0/guide/wiki/YAML) it publishes -- every string is single-quoted, with a note that the double quotes inside `versionFormat` belong to the Go template and survive a single-quoted YAML scalar.'
time: '2026-08-26T15:47:40.743789001Z'
