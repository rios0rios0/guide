name: 'Sync Docs'

on:
  pull_request:
    paths:
      - '*.md'
      - '**/*.md'

jobs:
  check:
    runs-on: 'ubuntu-latest'

    steps:
      - name: 'Checkout'
        uses: 'actions/checkout@v6'

      - name: 'Check TOC Sync'
        run: 'bash .github/workflows/sync-docs/check-toc-sync.sh'
